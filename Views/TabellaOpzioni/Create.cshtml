@model AiDbMaster.Models.Opzione

@{
    ViewData["Title"] = "Nuova Opzione";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h4 class="mb-0">
            <i class="bi bi-plus-circle text-primary"></i> Nuova Opzione
        </h4>
        <small class="text-muted">Aggiungi una nuova opzione di configurazione</small>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left"></i> Torna alla lista
    </a>
</div>

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0"><i class="bi bi-sliders me-2"></i>Dati Opzione</h5>
            </div>
            <div class="card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="NomeOpzione" class="form-label fw-bold"></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-tag"></i></span>
                            <input asp-for="NomeOpzione" class="form-control" placeholder="Es: SmtpServer, GiorniEmail...">
                        </div>
                        <span asp-validation-for="NomeOpzione" class="text-danger"></span>
                        <small class="form-text text-muted">Il nome deve essere univoco. Usa una convenzione tipo CamelCase.</small>
                    </div>

                    <div class="mb-4">
                        <label asp-for="ValoreOpzione" class="form-label fw-bold"></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-code-slash"></i></span>
                            <textarea asp-for="ValoreOpzione" class="form-control" rows="3" placeholder="Valore dell'opzione"></textarea>
                        </div>
                        <span asp-validation-for="ValoreOpzione" class="text-danger"></span>
                    </div>

                    <div class="d-flex justify-content-end gap-2">
                        <a asp-action="Index" class="btn btn-outline-secondary">Annulla</a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg me-1"></i> Salva
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
